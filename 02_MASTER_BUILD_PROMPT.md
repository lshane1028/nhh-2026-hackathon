# 《꽃판: GO!》 제작용 마스터 프롬프트

아래 프롬프트를 코딩 에이전트에게 `01_GAME_DESIGN_PLAN.md`와 함께 제공한다. 첫 응답에서는 게임을 만들지 않고 설계·범위·테스트만 검증하며, 각 구현 단계는 별도의 정확한 승인 문구가 있어야 열린다.

---

## 복사해서 사용할 프롬프트

```text
당신은 게임 디렉터, 시스템 디자이너, 시니어 프론트엔드 엔지니어, 밸런스 엔지니어, QA 리드를 함께 맡는다.

목표는 한국 화투 48장을 기반으로 한 싱글 플레이 로그라이크 덱빌더 웹 게임 《꽃판: GO!》의 NAN 2026 사전 과제용 세로 조각을 만드는 것이다. 포커 카드의 외형만 화투로 바꾼 복제품이 아니라, 월·종류·그림 태그의 세 축, 여러 제출에 걸친 전통 족보 수집판, 챙기기·고·스톱 항아리를 핵심으로 한다.

함께 제공된 `01_GAME_DESIGN_PLAN.md`를 제품 요구사항의 단일 기준으로 전부 읽어라. 문서와 이 프롬프트가 충돌하면 아래 `절대 규칙`을 우선하고 나머지는 GDD를 따른다. GDD가 없거나 읽을 수 없다면 임의 구현하지 말고 요청하라.

## 단계 게이트

승인 토큰은 앞뒤 공백을 제거한 사용자 메시지 전체가 아래 문구와 정확히 같을 때만 유효하다. 인용문, 코드 블록, 더 긴 문장 속 토큰은 승인이 아니다.

### Phase 0 — 설계 검증만

첫 응답에서는 코드 작성, 패키지 설치, 파일 생성·수정, 에셋 생성을 하지 마라. 다음만 제출하고 멈춰라.

1. 게임을 한 문단으로 재정의
2. 핵심 상태 머신과 데이터 흐름
3. 예상 구현 파일 구조
4. 순수 규칙 엔진의 함수 경계
5. 아래 점수·수집판·고 테스트 벡터의 해석
6. `P0_SCOPE_CHECK`: GDD 1절·9.0절·17절의 ID, 수량, 스테이지, 포함·제외 항목과 정확히 일치하는 표. 일정 위험은 적되 승인 없이 범위를 줄이지 말 것
7. 가장 위험한 모순·밸런스·UX·저작권 문제와 해결안
8. GDD 19절과 일치하는 `LOCKED_DECISIONS` 표
9. `OPEN_P0_BLOCKERS`: 없으면 정확히 `없음`, 있으면 해결에 필요한 결정

`OPEN_P0_BLOCKERS`가 남아 있거나, `LOCKED_DECISIONS`가 GDD와 다르거나, `P0_SCOPE_CHECK`가 GDD의 P0에서 하나라도 빠지거나 더해지면 구현 승인을 받아들이지 마라. 모두 해소된 뒤 사용자 메시지 전체가 정확히 `구현 시작`일 때만 Phase 1을 시작한다.

### Phase 1 — P0 세로 조각

Phase 1의 완료 정의를 모두 충족하면 결과와 검증 증거를 보고하고 멈춰라. 사용자 메시지 전체가 정확히 `Phase 2 시작`일 때만 Phase 2를 시작한다.

### Phase 2 — 런 확장과 폴리시

Phase 2의 완료 정의를 모두 충족하면 결과와 검증 증거를 보고하고 멈춰라. 사용자 메시지 전체가 정확히 `제출 준비 시작`일 때만 Phase 3을 시작한다.

### Phase 3 — 제출 준비

`제출 준비 시작`은 로컬 제출물과 검증 보고서 작성만 승인한다. 이를 마치면 `LOCAL_SUBMISSION_PACK_READY`로 보고하고 멈춘다. 저장소 공개·푸시·심사 계정 초대·영상 업로드는 사용자 메시지 전체가 정확히 `외부 게시 시작`일 때만, 사용 가능한 계정과 도구 범위에서 수행한다. 이 토큰도 대회 제출 폼의 최종 전송을 승인하지 않으며, 최종 전송은 대상과 제출 내용을 명시한 별도 사용자 확인이 필요하다. 사용자가 직접 게시한다면 받은 최종 링크만 검증하고 `FINAL_LINKS_VERIFIED` 여부를 보고한다.

## 절대 규칙

### 카드와 행동

- 시작 덱은 1~12월 각 4장, 총 48장이다.
- 물리 구성은 광 5, 열끗 9, 띠 10, 피 24다. 쌍피는 물리 카드 1장이지만 피값 2다.
- 종류 기본 끗은 광 20, 열끗 10, 띠 5, 피 1, 쌍피 역할 2다. 월 숫자는 족보 판정에만 쓰며 끗에 다시 더하지 않는다.
- 손패 크기 8, 제출 기회 4, 버리기 기회 4다. 한 행동에서 1~5장을 제출하거나 버린다.
- 제출·버린 카드는 그 판의 뽑기 더미로 돌아오지 않고, 다음 판 시작 때 영구 변경이 반영된 덱으로 다시 섞인다.
- P0 카드의 단일 원본 필드는 `instanceId`, `originId`, 현재 `month`, 현재 `kind`, `ribbonGroup`, `chaffValue`, `tags`, `permanentKkeutBonus`다. `monthName`, `season`, 종류 기본 끗은 파생한다. 저장한다면 레지스트리 검증으로 파생값과 일치시킨다.
- 화공패로 종류를 바꾸면 그 종류의 기본 끗도 바뀌지만 영구 보너스는 보존한다. 피를 띠로 바꾸면 홍·초·청 그룹을 함께 고른다.
- 9월 술잔은 매 제출 미리보기에서 열끗(10끗) 또는 쌍피(피값 2, 2끗) 역할 중 하나를 사용자가 고른다. 선택은 카드 원본을 영구 변경하지 않으며 P0에서는 동시에 두 역할을 할 수 없다.
- 12월과 1월은 기본 연월로 이어지지 않는다. `t_leap_calendar`가 있을 때만 이어진다.

### 즉시 족보, 수집 족보, 점수

- 한 제출에서는 즉시 족보 하나만 메인으로 삼아 기본 끗·흥을 받는다. 그 족보에 참여하지 않은 여분 카드는 즉시 카드 끗을 내지 않는다.
- 제출한 모든 카드는 즉시 족보 참여 여부와 무관하게 전통 수집판 진행에 사용한다.
- 수집 족보를 새로 완성한 제출은 해당 +끗·+흥을 즉시 점수에 함께 준다. 확정 완료된 같은 수집 족보는 그 판에서 재발동하지 않는다. 고 실패로 미확정 완료가 롤백되었다면 아직 쓰지 않은 다른 복사 카드로 다시 완성할 수 있다.
- 한 제출에서 광 3·4·5단계를 동시에 처음 충족하면 가장 높은 단계 하나만 발동한다. 이전에 삼광을 확정한 뒤 나중에 사광을 달성한 경우에는 사광이 새로 발동할 수 있다.
- 수집 진행과 완성 기록은 카드 ID와 완료 족보 ID를 담은 확정 `C`, 현재 고 체인의 미확정 `Q`로 나눈다. 선택한 즉시 족보와 새 수집 족보의 숙련은 미확정 이벤트 `M`에 기록한다. 챙기기·스톱·강제·자동 정산 시 Q와 M을 확정하고, 고 실패 시 Q·M만 잃는다.
- 가능한 모든 즉시 족보 후보를 카드·레벨·수집 완성·부적까지 끝까지 계산한다. 기본 선택은 실제 최종 예상 점수가 가장 높은 후보이며, 동점은 최종 끗, 레지스트리 순서, ID 순으로 결정한다. 플레이어는 제출 전에 다른 후보를 수동 선택할 수 있다.
- 족보 레벨 기본값은 `Lv1 + (level-1)×성장값`이다. 중간의 0.25흥은 보존하고 최종 손 점수에서만 내림한다.
- 손 점수는 정확히 `floor(최종 끗 × 최종 흥)`이며 UI에 두 칸으로 표시한다.
- 처리 순서는 `즉시 족보 기본값 → 새 수집 족보 완성값 → 득점 카드 왼쪽부터 → 손에 남은 카드 왼쪽부터 → 부적 왼쪽부터 → 최종 floor → 고 항아리`다.
- `+흥`은 처리 시점의 현재 흥에 더하고 `×흥`은 그 시점의 현재 흥에 곱한다. 부적 배치 순서가 결과에 반영되어야 한다.
- 점수 로그는 UI 문장이 아닌 구조화된 이벤트 배열로 먼저 만들고 UI가 렌더링한다.

### 챙기기·고·스톱 항아리

- 상태는 목표 `T`, 확정 점수 `B`, 미확정 항아리 `P`, 확정 수집 `C`, 미확정 수집 `Q`, 미확정 숙련 `M`, 현재 체인의 성공 고 횟수 `G(0~3)`, 다음 누적 문턱 `R`, 이미 정산한 성공 고 누계 `A`를 가진다.
- 체인이 없을 때 제출한 손 점수 H를 P에, 카드·미확정 완료 ID를 Q에, 숙련 이벤트를 M에 넣는다. 현재 정산 예상값은 `S=floor(P×보너스[G])`다.
- 현재 항아리를 정산해도 목표 또는 두목 추가 조건을 충족하지 못하면 `챙기기`와 `고`를 보여준다. 현재 항아리까지 정산하면 목표와 두목 조건을 모두 충족할 때만 `스톱`과 `고`를 보여준다. 일반판은 `B+S≥T`, 고집쟁이 판은 추가로 `A+G≥1`이어야 한다. `스톱`을 안전 저장 버튼 이름으로 사용하지 않는다.
- 모든 안전 정산은 하나의 원자 함수로 `S=floor(P×bonus[G])`, `B'=B+S`, `C'=C∪Q`, `A'=A+G`, M 숙련 커밋을 수행한 뒤 `P=0,Q=∅,M=∅,G=0,R=null`로 초기화하고 B'·A'로 승리를 판정한다. 챙기기·스톱·강제·자동 정산이 이 함수를 정확히 한 번 호출한다.
- 고를 누르면 `next=G+1`, `R=calculateGoRequirement(P,T,next)`를 저장하고 결정 뒤 8장까지 보충한다. 남은 손패와 덱 수는 보이지만 보충될 정확한 카드는 숨긴다.
- UI의 `한 장 완성패 N장`은 남은 덱의 각 카드 한 장을 현재 손에 가상으로 더해 합법적인 1~5장 제출을 전부 평가했을 때 `R-P` 이상을 만드는 카드 수다. 여러 장 보충의 전체 성공 확률이 아닌 보수적인 한 장 아웃 수임을 표시한다.
- 고가 걸린 동안에도 버리기를 허용한다. 버리기는 P·Q·M·G·R을 바꾸지 않고 버리기 횟수만 소비한다.
- 다음 제출 뒤 `P+H≥R`이면 정확히 같은 경우까지 성공이다. P에 H, Q에 카드·완료 ID, M에 숙련 이벤트를 더하고 G를 1 올린 뒤 R을 null로 만든다.
- `P+H<R`이면 실패다. 실패 손을 포함한 P·Q·M을 잃고 `P=0,Q=∅,M=∅,G=0,R=null`로 초기화한다. B, C, A, 사용한 제출·버리기 횟수는 보존한다.
- 고는 `P≥T×0.15`이고 제출 기회가 남아 있을 때만 가능하다. G=3이면 남은 제출과 관계없이 더 고할 수 없다.
- 제출 처리 우선순위는 `고 실패 → 남은 제출 0이면 AUTO_SETTLE 1회 → 그 외 G=3이면 FORCE_SETTLE 1회 → 그 외 선택 화면`이다. 마지막 손 3고도 이중 정산하지 않는다.
- 마지막 제출의 고 실패는 항아리를 소멸시킨 뒤 기존 B·C·A만으로 승리를 검사한다. 마지막 0고와 성공한 고 체인은 원자 자동 정산 후 목표·두목 조건을 검사한다.
- G=3 강제 정산 뒤 목표 미달이고 제출이 남았다면 B·C를 확정하고 새 체인으로 계속할 수 있다.
- 문턱은 올림, 정산은 내림한다.
  - 1고: `R=ceil(max(P×1.75, P+T×0.12))`, 성공 정산 `floor(P×1.15)`
  - 2고: `R=ceil(max(P×1.60, P+T×0.18))`, 성공 정산 `floor(P×1.35)`
  - 3고: `R=ceil(max(P×1.50, P+T×0.25))`, 성공 정산 `floor(P×1.70)`
- 고 보너스는 손의 흥과 분리하며 챙기기·스톱·강제·자동 정산 때 항아리에 한 번만 적용한다.
- 고가 한 번 이상 성공한 체인에서 새 수집 족보를 안전 정산하면 정상 완성 숙련 +1에 추가 숙련 +1과 1냥을 더한다. 고 실패 시 둘 다 없다.

### 재현성, 콘텐츠, 권리

- 게임의 모든 랜덤은 시드 PRNG 하나를 주입받는다. 규칙 코드에서 `Math.random()`을 직접 호출하지 않는다.
- 족보·부적·화공패·비결서·두목·목표 점수는 타입이 있는 데이터 레지스트리로 분리한다. P0에 미래 콘텐츠용 범용 DSL이나 사용하지 않는 추상화를 만들지 않는다.
- 실제 돈, 환전, 현금성 보상, 사용자 간 베팅, 현금 결제는 넣지 않는다. `냥`은 한 런 안에서만 쓰는 가상 재화다.
- 특정 상업용 화투 카드 스캔이나 Balatro의 아트·UI·사운드·명칭·문구를 복제하지 않는다.
- 로그인, 런타임 유료 AI API, 참가자의 유료 계정 없이 심사자가 링크 클릭만으로 플레이할 수 있어야 한다.
- 첫 코드·에셋 생성 전에 AI 로그·프롬프트·에셋·오픈소스 장부를 만든다. 이후 생성·수정 작업마다 날짜, 모델·버전, 프롬프트 ID, 사람의 채택·수정·기각 판단, 관련 파일·커밋, 출처·라이선스·SHA-256을 갱신한다.

## 기본 기술 선택

기존 저장소에 기술 스택이 있으면 호환성과 사용자 변경 보존을 우선한다. 빈 저장소라면 다음을 기본으로 한다.

- React + TypeScript + Vite 정적 웹 앱
- 규칙 엔진은 프레임워크·DOM과 분리한 순수 TypeScript
- 작은 명시적 reducer/state machine. 필요하지 않으면 대형 상태 라이브러리를 추가하지 않음
- Vitest, TypeScript strict, ESLint
- CSS 변수와 반응형 레이아웃: 1280×720, 390×844에서 핵심 행동이 모두 가능
- 버전이 있는 localStorage 저장과 손상 저장 복구
- GitHub Pages의 하위 경로 base에서도 직접 접속·새로고침 가능
- 제출 빌드에 사용한 패키지 잠금 파일 유지

## 상태 머신

BOOT
→ TITLE
→ RUN_SETUP
→ ROUND_INTRO
→ DEAL
→ SELECT_ACTION
   ├─ DISCARD_RESOLVE → REFILL → SELECT_ACTION
   └─ HAND_SCORE_PREVIEW → HAND_SCORE_RESOLVE
       ├─ armed 상태에서 P+H<R → GO_FAIL → 남은 제출/확정 점수 검사
       └─ 새 체인 또는 고 성공
           ├─ 제출 0회: AUTO_SETTLE exactly once
           ├─ 그 외 G=3: FORCE_SETTLE exactly once
           └─ SETTLEMENT_DECISION
               ├─ 승리 조건 미달: BANK(챙기기) 또는 GO_ARMED
               └─ 정산 시 승리 조건 충족: STOP 또는 GO_ARMED
→ ROUND_WIN
   ├─ 1·2월: REWARD → GUARANTEED_SHOP → NEXT_ROUND
   └─ 3월: BOSS_RESULT → RUN_WIN
→ RUN_LOSE

`GO_ARMED` 중 `DISCARD_RESOLVE`가 가능해야 한다. 애니메이션 상태는 규칙 상태와 분리하고, 연출을 건너뛰어도 동일한 시드에서 결과가 같아야 한다.

## 순수 엔진의 최소 함수 경계

- `createStandardHwatuDeck(): CardInstance[]`
- `validateStandardDeck(deck): ValidationIssue[]`
- `shuffleWithSeed(deck, seed): CardInstance[]`
- `getEffectiveCardRole(card, actionChoice): EffectiveCardRole`
- `findImmediateYakuCandidates(submitted, context): YakuCandidate[]`
- `getCollectionProgress(confirmed, pending, submitted, context): CollectionProgress[]`
- `detectNewCollectionCompletions(input): CollectionCompletion[]`
- `getScoringCards(candidate, submitted): CardInstance[]`
- `evaluateCandidateScore(candidate, input): ScoreBreakdown`
- `chooseDefaultCandidate(evaluated): EvaluatedCandidate`
- `calculateHandScore(input): ScoreBreakdown`
- `calculateGoRequirement(pot, target, nextGoLevel): number`
- `getSettlementValue(pot, successfulGoCount): number`
- `countSingleDrawOuts(hand, remainingDeck, requiredHandScore, context): OutCountResult`
- `resolveGoAttempt(state, handScore, submitted): GoResolution`
- `settleChainAtomically(state, reason): SettlementResolution`
- `commitMasteryEvents(events, context): MasteryState`
- `bankChain(state): RoundScoreState`
- `stopRound(state): RoundResolution`
- `forceOrAutoSettle(state): RoundResolution`
- `applyPainterCard(effect, runState, selection): RunState`
- `applyBook(effect, runState): RunState`
- `applyShopPurchase(item, runState): RunState`
- `validateContentRegistry(registry): ValidationIssue[]`

함수는 입력을 변이하지 않고 가능한 한 입력·출력만으로 테스트한다. 카드 인스턴스 ID, 족보 완성 ID, 콘텐츠 ID는 저장·로드 뒤에도 안정적이어야 한다.

## 시작 덱 검증

월별 네 장을 정확히 테스트하라.

1월 광·홍단·피·피
2월 열끗(새)·홍단·피·피
3월 광·홍단·피·피
4월 열끗(새)·초단·피·피
5월 열끗·초단·피·피
6월 열끗·청단·피·피
7월 열끗·초단·피·피
8월 광·열끗(새)·피·피
9월 열끗(술잔)·청단·피·피
10월 열끗·청단·피·피
11월 광·쌍피·피·피
12월 비광·열끗(새)·비띠·쌍피

필수 합계:
- 전체 48, 월별 4
- 광 5 / 열끗 9 / 띠 10 / 물리 피 24
- 쌍피 2
- 홍단 3 / 초단 3 / 청단 3 / 비띠 1
- 기본 새 태그는 2·4·8월 고도리를 정확히 식별

## P0 콘텐츠 매니페스트

Phase 1에서는 아래 ID만 구현한다. GDD의 나머지 콘텐츠와 미래 효과 시스템은 넣지 않는다.

즉시 족보 8:
`single`, `month_pair`, `two_pairs`, `three_run`, `chaff_field`, `triple_month`, `four_ribbons`, `four_animals`

수집 족보 8:
`hongdan`, `chodan`, `cheongdan`, `godori`, `rain_three_brights`, `three_brights`, `four_brights`, `five_brights`

부적 6:
- `t_chaff_bind` 피붙이, 5냥, 흔함 25
- `t_ribbon_maker` 띠장이, 5냥, 흔함 25
- `t_animal_tracks` 산짐승 발자국, 5냥, 흔함 25
- `t_bright_polish` 광약, 6냥, 흔함 20
- `t_pair_knot` 짝패 매듭, 6냥, 흔함 20
- `t_leap_calendar` 윤달 달력, 7냥, 드묾 10

화공패 4:
- `p_overpaint` 덧칠, 4냥, 흔함 25
- `p_double_chaff` 쌍피, 4냥, 흔함 25
- `p_ribbon_dye` 색동띠, 4냥, 흔함 25
- `p_bird_mark` 새김, 5냥, 드묾 15

비결서 5:
- `b_month_pair` 월쌍 비결, 4냥, 흔함 25
- `b_three_run` 삼연월 비결, 4냥, 흔함 20
- `b_chaff_field` 피밭 비결, 4냥, 흔함 20
- `b_four_ribbons` 띠다발 비결, 5냥, 드묾 15
- `b_four_animals` 열끗잔치 비결, 5냥, 드묾 15

정확한 P0 효과:
- `t_chaff_bind`: 득점 피값 1마다 +3끗
- `t_ribbon_maker`: 득점 띠마다 +5끗
- `t_animal_tracks`: 득점 열끗마다 +6끗
- `t_bright_polish`: 득점 광마다 +10끗
- `t_pair_knot`: 월쌍 또는 두쌍이면 +2흥
- `t_leap_calendar`: 연월 족보에서만 12월과 1월 연결
- `p_overpaint`: 선택 카드 1장이 다른 선택 카드의 월을 복사
- `p_double_chaff`: 일반 피 1장을 쌍피로 변경
- `p_ribbon_dye`: 피 또는 띠 1장을 띠로 만들고 홍·초·청 그룹 선택. 피였다면 종류 기본 끗도 5로 변경
- `p_bird_mark`: 피 1장을 새 열끗으로 만들고 월을 2·4·8월 중 하나로 선택. 종류 기본 끗은 10
- 모든 `b_*`: 대응 즉시 족보의 이번 런 레벨 +1

두목은 `boss_stubborn` 고집쟁이만 구현하며, 목표 점수 외에 성공한 고를 포함해 정산한 기록이 최소 1회 있어야 3월을 클리어한다. `boss_falling_first` 낙화는 P0 밖의 스트레치이며 데이터 스텁도 만들지 않는다.

튜토리얼 첫 장터는 `t_pair_knot`, `t_ribbon_maker`, `t_animal_tracks` 중 하나를 무료로 고르게 한 뒤 각각 `b_month_pair`, `b_four_ribbons`, `b_four_animals`를 무료 사용해 경로 족보의 레벨 상승을 보여준다. 두 번째 장터는 각각 `p_overpaint`, `p_ribbon_dye`, `p_bird_mark` 한 장만 구매 가능하게 하고, 1·2월 보상으로 반드시 살 수 있어야 한다. 경로마다 시드로 검증한 변형 후보 카드 3장만 보여주며, 어느 후보를 골라도 바뀐 카드가 3월 첫 손에서 월쌍, 띠다발, 고도리/열끗잔치의 실제 득점 조합과 함께 등장한다. 일반 모드만 자유 대상과 세 상품을 쓴다. 3월 뒤에는 장터 없이 결과로 가고, 튜토리얼 첫 실패에 한해 같은 덱·시드로 3월만 한 번 연습 재도전할 수 있다.

## 필수 점수·수집판 테스트 벡터

별도 언급이 없으면 레벨 1, 부적·강화 없음, C·Q가 비어 있다.

1. 1월 광 + 1월 피의 월쌍:
   `(월쌍 기본 10 + 카드 20+1) × 2흥 = 62`

2. 2·4·8월 새 열끗 3장을 한 번에 처음 제출:
   즉시 족보는 홑패, 수집 족보 고도리 완성이다.
   `(홑패 기본 5 + 최고 카드 10 + 고도리 32) × (1+5)흥 = 282`

3. 비광을 포함한 광 3장을 한 번에 처음 제출:
   `(홑패 기본 5 + 최고 카드 20 + 비삼광 32) × (1+4)흥 = 285`

4. 비광 없는 광 3장을 한 번에 처음 제출:
   `(홑패 기본 5 + 최고 카드 20 + 삼광 38) × (1+5)흥 = 378`

5. 오광을 한 번에 처음 제출:
   광 단계 중 오광만 발동한다.
   `(홑패 기본 5 + 최고 카드 20 + 오광 90) × (1+8)흥 = 1,035`

6. C에 2·4월 새가 있고 8월 새 열끗 + 8월 피를 월쌍으로 제출하며, 부적이 순서대로 `+12끗`, `+2흥`, `×1.25흥`을 준다:
   `65끗 × 11.25흥 = floor(731.25) = 731`
   로그는 `끗: 10 + 10+1 + 32 + 12`, `흥: (2 + 5 + 2) × 1.25`다.

7. 족보에 참여하지 않는 여분 카드가 있어도 그 카드의 기본 끗은 더하지 않지만, 그 카드도 수집 진척에는 들어간다.

8. 여러 즉시 후보가 겹치면 부적까지 반영한 실제 최종 점수 최고 후보가 기본 선택된다. 수동으로 다른 후보를 고르면 미리보기와 제출 결과가 함께 바뀐다.

9. 챙기기·스톱·강제·자동 정산은 Q의 카드·완료 ID를 C로 옮기고 M의 숙련을 커밋한다. 고 실패는 Q·M만 지우고 기존 C·숙련을 유지한다.

10. `C={2월 새,4월 새}`에서 8월 새 한 장을 홑패로 내 고도리를 완성하면 282다. 그 체인이 실패한 뒤 아직 쓰지 않은 다른 8월 새 복사본으로 다시 완성하면 다시 282다. 첫 완성을 안전 정산한 뒤 다른 8월 새를 내면 고도리 보너스 없이 `(5+10)×1=15`다.

각 P0 즉시·수집 족보에는 최소 한 개의 양성 테스트, 한 개의 1장 부족 음성 테스트, 중복 카드가 빠진 월을 대신 채우지 못하는 테스트를 둔다.

## 필수 고 테스트 벡터

목표 T=6,000, 첫 손 H=1,230, B=0:

- 첫 손 뒤 P=1,230, 0고 즉시 챙기기 예상값은 1,230
- 1고 문턱은 `ceil(max(1,230×1.75, 1,230+6,000×0.12)) = 2,153`
- 다음 손 923이면 경계값과 같아 성공, 922면 실패
- 다음 손 1,000이면 P=2,230, G=1, 챙기기 예상값은 `floor(2,230×1.15)=2,564`
- 2고 문턱은 `ceil(max(2,230×1.60, 2,230+6,000×0.18)) = 3,568`
- 셋째 손 1,500이면 P=3,730, G=2
- 3고 문턱은 `ceil(max(3,730×1.50, 3,730+6,000×0.25)) = 5,595`
- 마지막 손 1,900이면 P=5,630, G=3
- 3고 강제 정산은 `floor(5,630×1.70)=9,571`이며 판을 이긴다.

추가 경계 테스트:
- 고를 누르는 순간 R을 저장하고, 성공 직후 R을 null로 지운 뒤 다음 고 선택에서 새 값을 저장한다.
- 고 실패 시 P·Q·M·G·R은 초기화되고 B·C·A와 사용 횟수는 보존된다.
- 여러 체인을 챙기면 B가 정확히 누적되고 이전 C가 유지된다.
- 일반판은 `B+S<T`에서 챙기기, `B+S≥T`에서 스톱을 표시한다. 두목판은 목표와 추가 조건을 정산 후 모두 만족할 때만 스톱을 표시한다.
- P가 T의 15%보다 1 작을 때 고 불가, 경계와 같을 때 제출이 남아 있으면 가능하다.
- G=3이면 고 버튼이 없고 강제 정산한다.
- 고가 걸린 중간에 버려도 P·Q·M·G·R은 동일하다.
- 마지막 제출은 자동 정산하며, 목표 미달은 패배다.
- `P=2,230,G=1` 자동 정산은 B에 2,564, A에 1을 정확히 한 번 더한다. `P=5,630,G=3`의 마지막 손 정산은 B에 9,571, A에 3을 한 번만 더하며 19,142가 되면 실패다. 마지막 0고 `P=1,230`은 B에 1,230을 더한다.
- 분기 A `월쌍 손 → 1고 선언 → 홑패 손으로 1고 성공 → 안전 정산`은 두 즉시 족보 숙련이 각각 +1이다. 분기 B는 동일한 정산 전 상태에서 2고를 선언하고 실패하며, 그 체인의 월쌍·홑패 숙련이 모두 +0이고 M이 비어야 한다. 강제·자동 정산도 같은 숙련 커밋을 쓴다.
- 고 성공 체인의 수집 족보 완성은 안전 정산 시 정상 +1과 한숨 완성 추가 +1로 총 숙련 +2, 추가 1냥이다.
- 고 선택 전 다음 누적 문턱, 다음 손 최소 점수, 실패 시 소멸 P, `한 장 완성패`의 정의와 수를 표시한다.

## Phase 1 작업 범위

`구현 시작` 승인 뒤 다음 순서로 진행한다.

1. 저장소·기존 변경·라이선스를 읽고 사용자 변경을 보존한다.
2. 어떤 코드·에셋보다 먼저 `docs/AI_USAGE_LOG.md`, `docs/PROMPTS/`, `docs/BALANCE_REPORT.md`, `docs/ASSET_LICENSES.md`, `docs/THIRD_PARTY_NOTICES.md`, `docs/SUBMISSION_GAME_OVERVIEW.md`, `docs/SUBMISSION_AI_TECH.md`, `assets/manifest.json`의 추적·제출 초안 골격을 만든다.
3. 기술 스캐폴드, 품질 명령, 시드 PRNG, 버전 저장 스키마를 만든다.
4. 48장 데이터, P0 족보·콘텐츠 레지스트리, 순수 점수·수집판·고 엔진과 테스트를 만든다.
5. 8장 손패, 제출 4, 버리기 4, 후보 선택, 상황형 술잔 선택, 상세 산식, C/Q 수집판, 챙기기·고·스톱 UI를 만든다. HUD는 진척 수집 족보 최대 3개만 우선 노출한다.
6. 1월 300, 2월 480, 3월 800과 `boss_stubborn` 승패를 만든다.
7. P0 부적 6, 화공패 4, 비결서 5, 보장 장터 2회, 덱 보기, 영구 카드 변경을 만든다.
8. 고정 튜토리얼 시드에서 1월 월쌍·1고·챙기기 → 무료 부적·경로 비결서 → 2월 부적 발동·수집 진척 → 연계 화공패 구매·검증 후보 변형 → 3월 첫 손의 실제 연계 조합 → 두목 1고·스톱이 10~15분 안에 보이게 한다. 세 경로와 3월 1회 연습 재도전을 각각 검증한다.
9. 수집판이 포함된 새 엔진으로 다섯 정책 합계 최소 10,000 고정 시드를 돌리고, 서로 다른 시드 5런을 브라우저에서 완주해 목표 곡선을 검산한다.
10. 2초 안에 건너뛸 수 있는 최소 점수·도장 연출을 넣는다.
11. 브라우저에서 전체 동선을 직접 검증하고 제출 문서 초안·로그를 갱신한다.

사운드, 두 번째 두목, 손질, 묶음, 계약, 판본, 낙관, 금단패, 마당패, 폭탄, 흔들기는 Phase 1에서 구현하지 않는다.

### Phase 1 완료 정의

- 테스트, 타입체크, 린트, 프로덕션 빌드가 모두 종료 코드 0이다. 필수 테스트를 skip/only 처리하지 않는다.
- 시작 덱 합계, P0 모든 족보 양성·음성, 위 점수·수집판·고 경계 벡터가 전부 통과한다.
- 같은 시드는 덱 순서, 장터, 두목, 점수 이벤트를 동일하게 만든다.
- `docs/BALANCE_REPORT.md`에 다섯 정책 10,000시드와 브라우저 5런 결과가 있고, 1월 일반 시드 첫 플레이 클리어율 80~90%, 튜토리얼의 1고·3월 완주 100%를 확인한다. 2월 고 가능 상황·자발적 선택·성공·실패율, 고 회피/선호의 클리어율·평균 냥·최종 점수, 세 경로별 최종 점수를 함께 기록한다. 한 정책이 안정성과 보상을 모두 지배하면 완료가 아니다. `300/480/800`이 벗어나면 자동 변경하지 말고 증거와 새 목표를 승인 요청한다.
- 짝패·띠·열끗 세 튜토리얼 분기 모두 `구매 → 검증 후보 변형 → 3월 첫 손 연계 → 1고 성공 → 두목 스톱` 자동·브라우저 테스트를 통과한다.
- 콘텐츠 ID 중복·누락·알 수 없는 참조가 없고 카드 ID가 저장·로드 뒤 유지된다.
- 손상되거나 구버전인 저장 데이터는 빈 런으로 안전 복구하며 화면이 멈추지 않는다.
- 1280×720과 390×844에서 가려진 필수 버튼이 없고 키보드 포커스·터치가 동작한다.
- 실제 브라우저 콘솔에 처리되지 않은 예외·React 경고·404 자산 오류가 없다.
- 고정 시드가 제목 화면부터 3월 결과까지 10~15분 안에 완주된다.
- GitHub Pages 하위 경로용 프로덕션 빌드를 로컬 프리뷰에서 직접 접속·새로고침 검증한다.
- 최소 점수·도장 연출이 2초 안에 끝나고 건너뛰어도 규칙 상태가 같으며, 두 제출 PDF의 Markdown 편집 원본이 현재 빌드와 일치한다.
- 완료 보고에 변경 파일, 실행한 명령과 종료 결과, 브라우저 검증 화면, 남은 제외 항목, AI·에셋 기록 위치를 포함한다.

## Phase 2 작업 범위

`Phase 2 시작` 승인 뒤 GDD의 P1만 확장한다.

- 4~6월과 여름 두목까지 런 확장
- 부적 36종 데이터와 조합 안전 검사
- 계절 계약, 묶음, 시작 덱 해금
- 카드 손질 전체와 덱 필터
- 두목 총 8종 중 범위에 필요한 규칙
- 승패·시드·빌드 통계와 자동 밸런스 봇
- 접근성·연출·성능 폴리시

Phase 2에서도 P2의 마당패·폭탄·흔들기·박·날씨·판본·낙관·금단패는 별도 승인 없이는 넣지 않는다.

### Phase 2 완료 정의

- Phase 1 회귀 테스트와 새 콘텐츠 테스트가 모두 통과한다.
- 같은 시드 재현성과 저장 마이그레이션이 유지된다.
- 부적 조합에 무한 재발동, 음수 비용, NaN·Infinity 점수가 없다.
- 무작위·탐욕·족보 집중·고 회피·고 선호 정책을 고정 시드 묶음으로 실행하고 결과를 `docs/BALANCE_REPORT.md`에 남긴다.
- 실제 브라우저에서 1~6월 한 런과 모바일 핵심 동선을 검증한다.

## Phase 3 제출 하드 게이트

`제출 준비 시작` 승인 뒤 GDD 18절의 `SUBMISSION_ACCEPTANCE`를 축약하지 말고 그대로 체크한다. 한 항목이라도 미충족이면 `제출 준비 완료`라고 말하지 말고 정확한 미충족 항목과 해결 방법을 보고한다.

- 플레이 빌드와 전체 소스: 웹은 설치·로그인·유료 API 없이 링크에서 즉시 실행. 모바일이면 심사자가 설치·테스트 가능. `.exe` 단독 제출 금지
- 저장소: 빌드와 같은 전체 소스, 잠금 파일, 의미 있는 커밋 이력. 공개 권장, 비공개면 `dl_gameai_reviewer@nhn.com` 초대
- 실제 플레이 중심 30~60초 YouTube 영상: 공개 또는 링크 공개 상태
- 게임 소개·조작·규칙·실행 방법 PDF
- 도구·모델·주요 프롬프트·구체적 활용·사람의 판단·외부 에셋·오픈소스 출처를 포함한 AI 활용 기술 PDF
- 2인 이상이면 팀별 실제 역할·산출물 PDF, 1인은 불필요
- 새 시크릿 창과 다른 기기에서 모든 링크 확인, 심사 기간 동안 권한 유지
- 마감 뒤 심사 대상 링크·소스·영상 변경 금지, D-1 공식 마감 시각 재확인
- 이미지·폰트·음원·코드의 상업적 이용·제출 권리와 회사·학교·팀 IP 승인 확인

실제 게임 화면을 녹화하고 AI 합성 시연 영상을 만들지 않는다. Phase 3 로컬 작업만 끝났다면 `LOCAL_SUBMISSION_PACK_READY`, 게시된 링크까지 모두 검증했다면 `FINAL_LINKS_VERIFIED`라고 구분해 보고한다. 마감 시각과 최종 양식은 D-1에 NAN 2026 공식 페이지에서 다시 확인한다.

## UX 합격 조건

- 화투 그림을 몰라도 월·종류·기본 끗을 읽을 수 있다.
- 카드 선택 즉시 모든 즉시 후보, 기본 선택 후보, 수동 선택, 수집 진척, 전체 산식이 갱신된다.
- C와 Q는 색뿐 아니라 자물쇠·점선·문자로 구분한다.
- 고 버튼에 다음 누적 문턱, 다음 손 최소 점수, 실패 시 소멸 점수, 보수적인 `한 장 완성패` 수가 보인다. 이를 전체 성공 확률처럼 표시하거나 초록/빨강 위험 등급으로 단정하지 않는다.
- 챙기기와 스톱의 의미가 문구·버튼 상태에서 섞이지 않는다.
- 홍·초·청단을 색만으로 구분하지 않고 문자·아이콘을 함께 쓴다.
- 점수 연출은 건너뛸 수 있고 2초 안에 끝난다.
- 모바일 터치, 데스크톱 마우스·키보드로 핵심 플레이가 가능하다.
- 두목 규칙과 확률 효과는 결정 전에 정확한 수치로 공개한다.

## 작업 원칙

- 매 단계 전 기존 파일과 사용자 변경을 확인하고 보존한다.
- 작은 단위로 구현하고 각 단위 뒤 관련 테스트·타입체크·빌드를 실행한다.
- 실패한 테스트를 삭제하거나 기대값을 임의로 바꿔 통과시키지 않는다.
- 규칙을 바꿔야 한다면 변경 이유, 기존·신규 예시, 영향 받는 테스트를 먼저 보고하고 승인을 기다린다.
- 새 의존성은 필요성과 라이선스를 설명하고 최소한으로 추가한다.
- 임시 에셋도 직접 만든 SVG/CSS 또는 출처와 권리가 명확한 자산만 쓴다.
- 밸런스 숫자는 콘텐츠 데이터에 두고 코드 상수로 숨기지 않는다.
- 숫자나 규칙이 바뀌면 GDD, 콘텐츠 데이터, 테스트 벡터, AI 로그를 같은 변경에서 갱신한다.
- 범위 밖 기능을 “나중을 위해” 미리 구현하지 않는다.

이제 Phase 0 결과만 작성하고 멈춰라. 아직 파일을 만들거나 수정하지 마라.
```

---

## 사용 방법

1. 새 코딩 세션에 이 파일과 `01_GAME_DESIGN_PLAN.md`를 함께 제공한다.
2. 첫 답변의 `LOCKED_DECISIONS`, `P0_SCOPE_CHECK`, `OPEN_P0_BLOCKERS`를 검토한다.
3. P0 이견이 모두 해소된 뒤 메시지 전체로 `구현 시작`만 보낸다.
4. Phase 1 검증 결과를 확인한 뒤 필요할 때만 `Phase 2 시작`을 보낸다.
5. 제출 파일을 만들 시점에만 `제출 준비 시작`을 보낸다.
6. 에이전트가 외부 게시까지 맡아야 할 때만 `외부 게시 시작`을 별도 메시지로 보낸다. 대회 제출 폼의 최종 전송은 다시 확인한다.

구현 도중 수치를 바꾸면 GDD 표·콘텐츠 데이터·테스트 벡터·AI 로그를 함께 갱신한다. 코드와 문서가 서로 다른 규칙을 갖는 상태를 허용하지 않는다.
