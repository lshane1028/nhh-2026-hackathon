# 경화수월 — 현재 구현 인수인계

최종 갱신: 2026-08-12
상세 감사: [`docs/REPOSITORY_AUDIT_2026-08-12.md`](docs/REPOSITORY_AUDIT_2026-08-12.md)

이 문서는 현재 구현을 이어서 작업할 때 먼저 읽는 기준 문서입니다. 날짜가 붙은 `docs/HANDOFF_*.md`와 `docs/UI_ART_HANDOFF_*.md`는 당시 작업 기록이며, 규칙이나 수량이 충돌하면 코드·테스트와 이 문서를 우선합니다.

## 1. 게임 한 줄 요약

화투패를 짓과 끗패로 나눠 제출 점수를 만들고, 낸 패를 고스톱 수집판에 쌓아 수집 점수를 더하며, 부적·비결서·팩·계약으로 열두 달을 돌파하는 싱글 플레이 덱빌더입니다.

## 2. 실행과 검증

Node.js 22.13.0 이상이 필요합니다.

```bash
npm install
npm run dev
npm run verify
npm run build:pages
```

| 명령 | 용도 |
| --- | --- |
| `npm run dev` | 로컬 개발 서버 |
| `npm test` | Vitest 34개 파일, 244개 테스트 |
| `npm run typecheck` | TypeScript 검사 |
| `npm run lint` | ESLint |
| `npm run styles:check` | 대형 CSS 줄 수와 `!important` 증가 방지 |
| `npm run build` | vinext + Cloudflare 호환 빌드 |
| `npm run build:pages` | GitHub Pages 정적 export |
| `npm run verify` | test → typecheck → lint → build |

환경 변수, API 키, 데이터베이스는 필요하지 않습니다. 런타임 생성형 AI 호출도 없습니다.

## 3. 절대 바꾸지 말아야 할 규칙

### 선택 순서

- 한 번에 2~5장을 고릅니다.
- **처음 고른 두 장이 항상 끗패**입니다.
- 세 번째 이후에 고른 카드만 짓입니다.
- 카드를 해제한 뒤 다시 고르면 현재 `selectedCardIds` 순서가 기준입니다.
- 엔진이 더 높은 점수가 되는 분할을 자동으로 대신 고르지 않습니다.

### 끗패

- 끗패는 두 카드의 출력 월로 판정합니다.
- 같은 월 두 장은 카드 종류·강화 여부와 관계없이 땡입니다.
- 10월 두 장은 장땡, 11월 두 장은 11땡, 12월 두 장은 12땡입니다.
- 광땡은 13광땡, 18광땡, 38광땡 세 종류입니다.
- 그 밖의 우선순위는 광땡 → 땡 → 특수 족보 → 갑오 → 끗 → 망통입니다.

### 짓

- 짓 카드의 출력 월 합이 양수인 10의 배수여야 제출이 성립합니다.
- 짓이 없는 두 장 제출은 허용되며 월 합은 1에서 시작합니다.
- 금단패·화공패 등으로 월이 실제 바뀐 경우에는 바뀐 출력 월을 사용합니다.
- 단순 각인·판본·낙관·효과 표시는 원래 월의 동일성이나 땡 판정을 깨뜨리지 않습니다.

### 점수와 수집

```text
제출 점수 = floor(최종 월 합 × 최종 배수)
수집 점수 = 현재 고스톱 점수 × 20
이번 판 점수 = 누적 제출 점수 + 현재 수집 점수
```

- 제출 전 오른쪽 계산판은 기본 짓 월 합과 끗패 배수만 보여 줍니다.
- 부적과 카드 효과의 최종 결과는 제출 연출에서 순서대로 공개합니다.
- 제출 연출 중 제출·버리기는 잠기며, 최종 점수는 연출이 끝나기 전 갱신해 보이지 않습니다.
- 낸 카드는 실제 왼쪽 수집판으로 이동하며 광·동물·고도리·띠·피 유효 종류를 같은 helper로 계산합니다.
- 9월 술잔은 동물 또는 쌍피를 먼저 고른 뒤 해당 수집 결과와 판의 승패를 확정합니다.

### 고와 판 종료

- 목표를 넘으면 스톱하거나 최대 3고까지 선언할 수 있습니다.
- 고 문턱은 선언 시점 점수와 목표를 함께 사용해 고정합니다.
- 선언한 고 문턱을 남은 손으로 넘지 못하면 런이 끝납니다.
- 결산과 런 종료 통계는 이미 확정한 결과만 사용하며, 제출 연출보다 먼저 화면을 전환하지 않습니다.

## 4. 현재 콘텐츠 기준

`game/content/index.ts`의 검증 수량이 단일 기준입니다.

| 콘텐츠 | 수량 |
| --- | ---: |
| 표준 화투 | 48장 |
| 끗패 족보 | 14종 (공개 11 + 광땡 3) |
| 수집 족보 | 11종 |
| 부적 | 56종 |
| 비결서 | 22종 |
| 화공패 | 15종 |
| 금단패 | 10종 |
| 카드 효과 | 12종 |
| 각인 / 판본 / 낙관 | 8 / 4 / 4종 |
| 시작 덱 | 5종 |
| 우두머리 | 12종 |
| 상점 팩 | 11종 |
| 계절 계약 | 8쌍 |

카드 효과 12종은 모두 엔진에 연결되어 있습니다. `as_bright / as_animal / as_ribbon / extra_pi / twin_kind`는 수집 종류를, `partner_boost / heavy_month / echo / stubborn`은 득점을, `drawn_luck`은 드로우 후 버리기를, `gilded / keeper_coin`은 경제를 바꿉니다.

`열두 달의 화공`과 `오방 도깨비` 부적은 설명·조작 부담 때문에 현재 카탈로그에서 제거된 상태입니다. 저장 복원도 해당 구형 인스턴스를 제거합니다. 다시 넣기 전 전용 선택 UI와 예측 가능한 연출부터 설계해야 합니다.

## 5. 12개월 흐름

현재 목표는 다음과 같습니다.

```text
150 · 280 · 450 · 850 · 1,450 · 2,450
4,100 · 6,800 · 11,200 · 18,500 · 30,500 · 50,000
```

- 기본 제출 4회, 버리기 3회, 손패 8장, 부적 칸 5칸, 시작 4냥입니다.
- 3·6·9·12월이 보스 월이며 계절 결산 계약으로 이어집니다.
- 12월 뒤 무한 달력은 한 바퀴마다 목표 ×1.6입니다.
- 시작 덱은 정석패를 기본으로 3·6·9·12월 클리어 시 홍매패·청류패·먹산패·엽전패가 차례로 열립니다.

수집 점수의 현재 기준은 실제 고스톱식 점수입니다. 삼광 3점·비삼광 2점, 사광 4점, 오광 15점, 육광 22점, 동물·띠·피는 기준 수량 이후 증가하며 고도리·홍단·초단·청단과 상위 새떼 족보가 별도로 더해집니다. 자세한 현재값은 `game/engine/collection-bonus.ts`와 `docs/BALANCE_REPORT.md`가 기준입니다.

## 6. 장터와 팩

장터는 한 화면 네 구역입니다.

- 부적전: 부적 2개, 보유 부적은 다시 나오지 않음
- 비결서점: 기본 2권, 계약에 따라 1권 추가
- 덱 손질방: 카드·비결서·부적·소각 팩 중 3개
- 금단장: 금단패 1개, 직전 금단패 연속 등장은 방지

상품은 한 번 눌러 강조한 뒤 별도 구매 버튼으로 확정합니다. 카드나 버튼 바깥을 누르면 선택이 풀립니다. 보유 부적은 장터 상단에서 순서를 바꾸거나 판매할 수 있습니다.

| 팩 | 후보 / 선택 | 가격 |
| --- | --- | ---: |
| 카드 | 3/1, 5/2, 7/3 | 4, 8, 16냥 |
| 비결서 | 3/1, 5/2, 7/3 | 6, 11, 21냥 |
| 부적 | 3/1, 5/2, 7/3 | 8, 15, 28냥 |
| 소각 | 덱 전체에서 2장 또는 4장 확정 선택 | 5, 14냥 |

팩은 후보를 먼저 강조하고 `확정`을 눌렀을 때 한 번에 적용합니다. 최소 한 개를 골라야 확정할 수 있고 `그만 고르기`로 닫을 수 있습니다.

장터 진열은 `game/engine/economy.ts`의 시드 기반 가중 추첨을 사용합니다. 튜토리얼 고정 부적, 보유 부적 제외, 금단장 연속 중복 방지, 대서고 보장은 `game/state/market-actions.ts`와 `pack-actions.ts`의 보정 규칙으로 유지합니다.

## 7. 아키텍처

```text
app/                 React 화면·연출·오디오
  GameApp.tsx        상태 연결과 화면 라우팅
  hooks/             저장, 오디오, 제출, 금단장 연출 수명주기
  components/        카드, 수집판, 장터, 결산, 모달, 연출

game/
  content/           카드·족보·부적·상점·보스 선언 데이터
  engine/            React 비의존 순수 규칙 함수
  state/             GameState, GameAction, reducer, 저장 정규화
    selectors.ts     UI와 reducer가 공유하는 파생 상태
    market-actions.ts 장터 생성·진입·리롤
    pack-actions.ts  팩 후보 생성
    consumable-actions.ts 화공패·금단장 적용
    run-lifecycle.ts 계절 계약과 월 전환
  tests/             34개 파일, 244개 테스트

worker/              Cloudflare 응답과 보안 헤더, 이미지 최적화
scripts/             GitHub Pages export와 제작 도구
public/              실제 배포 자산
```

의존 방향은 다음을 지킵니다.

```text
app → game/state → game/engine + game/content
```

`game/`에서 React나 `app/`을 import하지 마십시오. UI는 액션을 dispatch하고, 게임 결과는 reducer와 엔진이 결정합니다.

## 8. 현재 구조 부채

- `app/GameApp.tsx`: 1,604줄. 주요 수명주기는 분리됐고 화면 라우터 경계가 남음
- `game/state/game.ts`: 1,503줄. 장터·팩·소모품·selector는 분리됐고 제출·라운드 수명주기 추가 분리가 남음
- `app/components/pixel-direction.css`: 2,465줄, `!important` 463개. CSS layer와 화면별 경계 필요
- CSS 이후에는 도감 발견 상태, 빌드 정체성 요약, 무한 달력 변주를 순서대로 검토

우선순위와 구체적인 추출 단위는 종합 감사 문서를 따릅니다. 이 부채를 해결하면서 규칙·화질·연출을 동시에 바꾸지 마십시오.

## 9. 카드와 아트 불변식

- 모든 카드형 그림은 2:3이며 `object-fit: contain`을 사용합니다.
- 11·12월을 포함한 48장 WebP가 기본이고, PNG와 공용 아틀라스는 실패 시 fallback입니다.
- 카드 전체가 3D로 기울며 선택·호버는 transform을 덮지 않고 CSS 변수로 합성합니다.
- 각인, 판본, 낙관, 효과는 서로 다른 시각 채널을 사용하고 카드 원화의 월과 종류를 가리지 않습니다.
- 부적과 카드 툴팁은 `document.body` 포털로 렌더하고 viewport 안으로 clamp합니다.
- 보스 계절 효과와 제출 연출은 `prefers-reduced-motion`에서 축소 또는 비활성화합니다.
- 생성 자산 연결은 `app/components/generated-asset.ts`, 카드 경로는 `app/components/hwatu-atlas.ts`가 기준입니다.

## 10. 저장과 배포

- 저장 키는 `flower-board-go:v2`입니다. 이전 브랜드 문자열이지만 기존 저장 호환 때문에 그대로 둡니다.
- 저장 실패는 게임 연출이나 진행을 막지 않도록 무해하게 처리합니다.
- 공개 GitHub Pages는 `main` push 시 `.github/workflows/pages.yml`이 배포합니다.
- Cloudflare Worker 경로는 CSP, COOP, CORP, Permissions-Policy, nosniff, frame 방지 헤더를 붙입니다.
- 정적 Pages는 같은 응답 헤더를 제공하지 않으므로 보안 요구가 커지면 Worker/Sites 배포를 주 경로로 검토합니다.

## 11. 작업 전 체크리스트

1. `docs/REPOSITORY_AUDIT_2026-08-12.md`에서 관련 부채와 불변식을 확인합니다.
2. 게임 규칙은 `game/engine`, 데이터는 `game/content`, 상태 전이는 `game/state`에서 고칩니다.
3. 표시와 계산이 중복되는 수집 종류·월·족보 helper를 새로 만들지 않습니다.
4. 시각 변경은 11·12월, 검은 면 카드, 효과 중첩, 부적 5개, 모바일 가로 화면을 함께 확인합니다.
5. 완료 전 `npm run verify`와 필요하면 `npm run build:pages`를 실행합니다.

## 12. 현재 검증 상태

2026-08-12 기준:

- 34개 테스트 파일, 244개 테스트 통과
- TypeScript 통과
- ESLint 통과
- vinext build 통과
- GitHub Pages static export 통과
- 개발 의존성 포함 `npm audit` 취약점 0건

추가로 1순위 감사 개선을 적용했습니다.

- Pages는 자산 검사·테스트·타입 검사·린트를 먼저 통과해야 배포됩니다.
- 장터 진열과 구매는 `game/engine/economy.ts`가 단일 기준입니다.
- `GameApp`의 저장·오디오·제출·금단장 연출과 세 모달을 분리했습니다.
- reducer의 selector·장터·팩·소모품·계절 전환을 도메인 모듈로 분리했습니다.
- 필수 자산 manifest와 36MiB 예산 검사를 추가하고 제작 원본은 `source-assets/`로 분리했습니다.
- `docs/ART_BIBLE.md`와 CSS 부채 예산 검사를 추가해 시각 규격과 `!important` 증가를 고정했습니다.
- 덱 보기·장터의 빌드 강점 태그와 보스전 레일의 상시 규칙·대응 안내를 추가했습니다.
- 덱 편집 기본 CSS를 `app/components/deck-editor.css`로 분리하고, 팩 확정 로직을 `game/state/pack-actions.ts`로 옮겼습니다.
- 무한 달력은 원래 보스 규칙을 보존하면서 떠돌이 두목 또는 날씨 변주를 결정론적으로 추가합니다.
- 손패 정렬·보충·선택은 `game/state/round-actions.ts`로 분리했으며, 무한 달력 변주가 실제 월 시작 상태에 전달되는 통합 테스트를 추가했습니다.
- 런타임 자산 예산은 `assets/runtime-manifest.json`, 검사는 `npm run assets:check`입니다.
- 원본 작업 자산은 `source-assets/`, 실제 배포 자산은 `public/`에 둡니다.
- `GameApp`의 저장과 오디오 장면 전환은 `app/hooks/`로 분리했습니다.
- 공통 모달은 초점 고정·복원·배경 inert를 처리하고, 부적은 좌우 방향키로도 순서를 바꿉니다.

Vite native config loader 미래 호환 경고는 제거했습니다.
