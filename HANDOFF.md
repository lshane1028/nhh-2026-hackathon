# 꽃판: GO! — 인수인계

이 문서 하나가 현재 상태의 유일한 기준이다. 다른 컴퓨터에서 클론해 이어서 작업할 때 이 문서부터 읽으면 된다. 예전에 있던 `01_GAME_DESIGN_PLAN.md`, `02_MASTER_BUILD_PROMPT.md`, `03_DEVELOPMENT_HANDOFF.md`는 지금 코드와 규칙이 달라져 삭제했다. 프롬프트 이력이 필요하면 `docs/PROMPTS/`에 남아 있다.

---

## 1. 한 줄 요약

기본 화투 48장으로 **짓고땡**을 만들어 점수를 내고, 목표를 넘긴 순간 **고**를 외칠지 **스톱**할지 고르는 싱글 플레이 로그라이크 덱빌더다. 웹에서 돌아가고 백엔드·계정·API 키가 필요 없다.

---

## 2. 실행

```bash
npm install
npm run dev          # http://localhost:3000
npm run verify       # test → typecheck → lint → build
```

Node.js `22.13.0` 이상. 진행 중인 런은 브라우저 `localStorage`(`flower-board-go:v2`)에 자동 저장된다. 규칙이 바뀔 때마다 이 키를 올려서 옛 저장을 버린다.

---

## 3. 규칙 — 짓고땡

전통 화투 놀이 짓고땡을 2~5장 제출로 일반화한 것이다. 제출한 패는 항상 **짓**과 **끗패** 두 덩어리로 갈린다.

### 끗패 (2장 · 배수)

섯다 족보 그대로다. 두 장의 월을 더한 **끝자리**가 끗수다.

| 족보 | 조건 | 기본 배수 |
| --- | --- | --- |
| 망통 | 끝자리 0 | 1 |
| 끗 | 끝자리 1~8 | 1 + 끗수×0.3 |
| 갑오 | 끝자리 9 | 4 |
| 세륙 | 4월·6월 | 4.5 |
| 장사 | 4월·10월 | 5 |
| 장삥 | 1월·10월 | 5.5 |
| 구삥 | 1월·9월 | 6 |
| 독사 | 1월·4월 | 6.5 |
| 알리 | 1월·2월 | 7 |
| 땡 | 같은 월 두 장 | 8 + (월−1)×0.6 |
| 장땡 | 10월 두 장 | 14 |
| 13광땡 | 1월 광·3월 광 | 16 |
| 18광땡 | 1월 광·8월 광 | 18 |
| 38광땡 | 3월 광·8월 광 | 20 |

판정 순서는 **광땡 → 땡 → 이름 붙은 특수패 → 갑오 → 끗 → 망통**이다. 이름 붙은 특수패가 숫자 판정을 이긴다(1·9는 합이 10이지만 망통이 아니라 구삥). 광땡 세 종은 비밀 족보라 한 번 만들기 전까지 목록에 뜨지 않는다.

### 짓 (나머지 · 월 합)

끗패를 뺀 나머지 카드의 월 합이 **10의 배수**여야 제출이 성립하고, 그 합이 그대로 월 합이 된다.

| 낸 장수 | 짓 | 성립 조건 |
| --- | --- | --- |
| 2장 | 없음 | 항상 성립. 월 합은 1에서 시작 |
| 3장 | 1장 | 그 카드가 10월 |
| 4장 | 2장 | 두 장의 월 합이 10 또는 20 |
| 5장 | 3장 | 세 장의 월 합이 10·20·30 |

두 장 제출을 항상 열어 둔 것이 진입 장벽을 낮추는 장치다. 점수는 낮지만 언제든 낼 수 있다. **예외는 이것 하나뿐이고, 다른 면제 규칙은 없다.**

월 값은 `getEffectiveMonth()`가 계산한다. 카드에 `permanentKkeutBonus`가 붙어 월이 +40이 되든 +50이 되든 그 값이 그대로 합에 들어가고, 조건은 여전히 `합 % 10 === 0`이다. 즉 화공패로 월을 키운 카드도 짓 규칙 안에서 그대로 굴러간다. 돌패는 항상 12, `zero_base` 태그는 0으로 친다.

과거에 있던 **흔들기·폭탄은 삭제했다.** 짓고땡에서는 같은 월 세 장의 짓이 10의 배수가 될 수 없어 제출 자체가 막히고, 그걸 뚫으려고 면제 규칙을 두는 순간 규칙이 하나 더 늘 뿐 재미가 늘지 않았다. 관련 상태(`pendingShakeChoice`, `shakeChoice`, `ExperimentalRules.bombsAndShake`, `YardState.shakeArmed`)와 `RESOLVE_SHAKE` 액션, `흔들쇠` 부적을 전부 걷어냈다. 부적 자리는 **짓모루**(짓 카드 한 장마다 배수 +1)로 대체했다.

### 점수

```
점수 = floor(월 합 × 배수)
```

- 월 합 = 짓의 합(짓이 없으면 1) + 카드 각인·판본 보너스 + 부적
- 배수 = 끗패 기본 배수 + 등급 보너스 + 수집판 완성 배수 + 부적

같은 카드로 짓과 끗패를 나누는 방법이 여러 가지면 **점수가 가장 높은 갈래**가 자동으로 선택된다. 플레이어가 족보를 고르는 UI는 두지 않는다.

### 고 · 스톱

목표를 넘긴 **순간에만** 결정 화면이 열린다. 그전까지는 계속 제출만 한다.

| 고 | 문턱 배율 | 판돈 배율 |
| --- | --- | --- |
| 0 | ×1.0 | ×1.0 |
| 1 | ×1.8 | ×1.7 |
| 2 | ×2.8 | ×2.7 |
| 3 | ×4.2 | ×4.2 |

문턱은 `ceil(max(목표×배율, 현재점수×1.5×배율))`이다. 뒤쪽 항이 **초과 달성 방지 장치**다. 이게 없으면 목표를 크게 넘긴 판에서 고가 공짜가 된다. 선언 시점에 `goRequirement`로 고정되어 이후 득점이 문턱을 움직이지 못한다. 3고가 최대이고, 남은 제출이 없으면 고를 걸 수 없다. 문턱을 못 넘기면 런이 끝난다(고박).

### 수집판 — 줄마다 다른 값을 낸다

다섯 줄이 전부 배수만 주던 때가 있었다. 그러면 수집판은 모자 다섯 개를 쓴 숫자 하나일 뿐이라 "어느 줄을 쫓을까"가 질문이 되지 않는다. 지금은 줄마다 다른 통화를 낸다.

**배수는 더하지 않고 곱한다.** "+5배수"는 이미 배수가 30인 판에서는 없는 것과 같지만 "×3.5"는 언제까지나 값이 산다. 줄 하나를 끝낸 값어치가 후반에도 사라지지 않아야 한다.

보상은 **만들기 어려운 순서**를 따른다. 피(24장이 널려 있다) → 띠·동물 5장 → 단 하나(특정 3장) → 고도리 → 3광 → 4광 → 5광(비광까지).

| 줄 | 조건 | 보상 |
| --- | --- | --- |
| 피 | 7점 초과 | 판돈 +0.5냥/점 |
| 피 | 10점 | 배수 **×1.5** |
| 띠 | 5장 | 배수 **×1.4** |
| 띠 | 단 하나마다 | 버리기 +1 |
| 띠 | 삼단 전부 (9장) | 배수 **×2.5** |
| 동물 | 5장 | 손패 +1 |
| 동물 | 8장 | 배수 **×1.8** |
| 고도리 | 3장 | **짓이 5의 배수여도 성립** (판 끝까지) |
| 광 | 3장 | 배수 **×1.6** · 고 문턱 −10% |
| 광 | 4장 | 배수 **×2.2** · 고 문턱 −20% |
| 광 | 5장 | 배수 **×3.5** · 고 문턱 −30% |

고도리만 숫자가 아니라 **규칙**을 준다. 8장 손패에서 합법적인 5장 조합이 65%에서 85%로 뛰므로 "낼 게 없다"는 상황이 거의 사라진다. 다른 어느 줄도 규칙을 바꾸지 않는다.

배수는 곱해서 쌓이지만 한 판에 담을 수 있는 카드가 20장 남짓이라 비싼 줄끼리는 사실상 양자택일이다. 그게 요점이다 — 수집판은 "이번 판에 어느 줄을 쫓을 거냐"를 묻는다.

버리기처럼 즉시 지급되는 것은 `submitHand`에서 **앞뒤 특전의 차이만** 더한다. 같은 줄이 두 번 완성돼도 두 번 주지 않게 하려는 것이다. 손패·고 문턱·짓 규칙은 매번 다시 읽으므로 `collectionPerks()` 하나만 보면 된다.

`multiplierBonus`는 이제 **합이 아니라 곱**이다. 1이면 수집판이 아직 아무것도 주지 않는다는 뜻이다.

한 판 안에서만 유지되는 지속 배수다. 홍단·초단·청단·고도리·비삼광·삼광·사광·오광 8줄. 광은 새로 도달한 **가장 높은 등급 하나만** 정산한다. 9월 술잔은 점수를 낸 뒤 동물/쌍피 중 어디에 기록할지 카드마다 한 번 고른다.

화면의 수집판은 **덱에서 만들어진다**(`game/engine/collection-board.ts`). 광·동물·고도리·띠 줄은 아틀라스에서 실제 카드 그림을 꺼내 그리고, 수집한 카드는 밝게 못 한 카드는 회색으로 죽여 그 줄이 그대로 체크리스트가 된다. 칸 수가 고정이 아니라 덱을 따라가므로 광을 소각하면 칸이 사라지고, 8월을 광으로 만드는 부적을 끼면 칸이 늘어난다.

같은 카드가 덱에 여러 장이면 그림 하나로 합치고 뒤에 카드를 겹쳐 그린 뒤 수를 붙인다. **겹친 그림이 먼저 보이고 숫자가 뒤따라야** 그 숫자가 점수 배수가 아니라 장수로 읽힌다. 일부만 모았으면 `1/2`, 전부 모았거나 하나도 없으면 `×2`로 쓴다.

**피 줄은 "가져온 것"만 그린다.** 덱에 24장이라 후보를 전부 깔면 판이 파묻히고, 어차피 어떤 피든 값이 같으니 체크리스트가 될 이유도 없다. `buildCollectionSlots({ collectedOnly: true })`로 집계 줄이 되고, 쌍피는 `2점` 배지로 왜 총합이 둘씩 뛰는지 밝힌다.

빈 목록은 그림 줄이 아니다 — 판이 시작될 때 피는 하나도 없으므로 `cards.length > 0`일 때만 그림을 그리고 아니면 칸으로 되돌아간다. 이 조건을 빼먹으면 피 줄이 통째로 빈 칸이 된다.

`buildCollectionSlots`의 `matchesKind`는 `collection-bonus.ts`의 같은 이름 함수를 그대로 복제한 것이다. **둘이 어긋나면 화면이 채점기가 세지 않는 카드를 약속하게 된다.** 한쪽을 고치면 반드시 다른 쪽도 고치고, `collection-board.test.ts`의 "agrees with the scorer" 테스트가 이걸 지킨다.

---

## 4. 확정된 방향 (되돌리지 말 것)

- 드래그 없음. 카드는 클릭으로만 선택/해제.
- 손패는 **항상 월 순**이다. 광·동물·띠·피 정렬 토글은 없앴다 — 짓이 월 합으로 만들어지므로 월 순 말고는 실제로 하는 일에 도움이 되지 않는다.
- 선택한 카드는 들어 올리고 금테를 두르고, 나머지 손패에 **옅은 그늘**을 얹는다. 카드 얼굴이 그림으로 꽉 차 있어 테두리 변화만으로는 안 보이기 때문이다. 다만 그늘은 약하게 — 세게 죽였더니 선택은 잘 보였지만 *다음에 뭘 고를지* 살펴야 할 나머지 패가 안 보였다.
- 족보는 항상 자동 최고점. 고르는 UI 금지.
- 시작 덱 선택 화면 없음. 모든 런은 `deck_standard` 48장으로 시작하고, 개성은 장터에서 만든다.
- 카드 얼굴은 그림 전체 + 왼쪽 위 월 숫자만. 나머지 정보는 호버 툴팁으로.
- **이미지를 생성하지 않는다.** 모든 자리는 `data-asset-tag` 텍스트 플레이스홀더로 두고, 사용자가 나중에 한 장씩 붙인다.
- 광을 삼광/사광/오광 상자 세 개로 쪼개지 않는다. 광 한 줄에 슬롯 5개.
- 고도리는 별도 줄로 표시하되 새 세 장은 동물 트랙에도 함께 집계한다.
- 별도 로그 패널·마당패 패널을 메인 화면에 다시 넣지 않는다.
- **덱은 뒤집힌 덱 더미를 눌러서 연다.** 화면에서 덱을 뜻하는 유일한 물건이 클릭에 반응하지 않는 게 이상했다. 레일의 "내 덱" 버튼은 없앴다.
- 덱 화면은 1월부터 12월까지 줄을 나눠 **실제 카드 그림**으로 보여 준다. `3월 · ribbon` 같은 글자 벽은 읽히지 않는다. 끗패 레벨·발견한 광땡 패널은 뺐다.
- 규칙 모달은 두 쪽이다 — 규칙 여섯 줄과, **실제 카드 그림으로 그린 끗패 족보표**.
- 한 화면 안에 다 들어와야 한다. 스크롤로 봐야 하는 정보를 만들지 않는다.

---

## 5. 파일 구조

```text
app/GameApp.tsx            화면 조합 · 리듀서 연결 · 모달
app/components/            프레젠테이션 컴포넌트 (props만 받음)
  HwatuCard.tsx            카드 한 장. 짓/끗패 표식과 호버 툴팁
  CollectionBoard.tsx      수집판. 실제 카드 그림 + 덱 기반 칸
  hwatu-atlas.ts           아틀라스 스프라이트 좌표 (손패·수집판 공용)
  PlayRail.tsx             점수판 레일 (월 합 × 배수 표시)
  MarketScreen.tsx         장터 한 화면 4분면
  TutorialSpotlight.tsx    발라트로식 스포트라이트 오버레이
  tutorial-steps.ts        튜토리얼 단계 정의
  game-ui.css              카드·수집판·부적
  screen-ui.css            타이틀·장터·런 종료
app/game.css               레이아웃 셸

game/types.ts              모든 공용 타입
game/content/              순수 데이터
  cards.ts                 48장 정의
  yaku.ts                  끗패 14종 + 수집 족보 8종
  stages.ts                1~12월 목표 곡선
  talismans.ts             부적 58종 (연계 조커 10 · 끗패 조커 5 포함)
  upgrades.ts              비결서 14 · 화공패 15 · 금단패 10 · 묶음 2 …
  card-effects.ts          카드 효과 태그 (라벨 전용, 아래 6절)
game/engine/               순수 함수. React 의존 없음
  yaku.ts                  짓/끗패 분할과 판정
  scoring.ts               월 합 × 배수 연산 파이프라인
  go.ts                    고 문턱 · 판돈 · 숙련
  talismans.ts             부적 효과 평가
  economy.ts               장터 뽑기 · 구매 · 리롤
  rng.ts                   시드 기반 결정론적 난수
  collection-board.ts      덱 → 수집판 칸
game/state/game.ts         런 리듀서 (제일 큰 파일)
game/state/storage.ts      버전 저장 (SAVE_KEY)
game/tests/                Vitest
```

---

## 6. 카드 효과 태그 — 아직 라벨이다

`game/content/card-effects.ts`의 12종 태그(광 취급, 피 +1, 쌍광 …)는 **화면에만 표시되고 엔진이 읽지 않는다.** 사용자가 나중에 직접 구현할 예정이라 의도적으로 비워 둔 것이다.

예외는 **고집패(`stubborn`) 하나**뿐이다. `isUndiscardable()`에서 실제로 버리기를 막는다. 다른 카드와 묶어 버려도 고집패만 손에 남는다.

`card.tags`를 이 용도로 재사용하지 말 것. `tags`는 `bird`·`rain`·`cup`처럼 종류와 족보 판정을 구동한다.

---

## 7. 밸런스

`game/tests/balance-probe.test.ts`가 기준이다. 부적 없이 8장 손패에서 가능한 모든 2~5장 조합을 완전 탐색해 4손을 낸 결과가 **라운드당 약 1,000점**이다. 사람은 완전 탐색이 아니므로 실제 무보정 상한은 400~600점으로 본다.

목표 곡선(스테이지 1~12):

```
150 · 290 · 510 · 950 · 1,800 · 3,400 · 6,400 · 12,000 · 22,000 · 41,000 · 76,000 · 140,000
```

- 1~3판: 장터 없이 맨손으로 넘긴다
- 4~6판: 무보정 상한을 넘어서므로 부적이 필요해진다
- 7~12판: 스테이지당 약 ×1.85. 뒤가 훨씬 가팔라진 건 카드 숫자를 키운 뒤 후반 상한이 같이 올라갔기 때문이다

### 월 합 숫자는 크게 잡는다

짓이 바탕을 10/20/30으로 묶어 두므로, "+3" 짜리 각인은 반올림 오차였다 — 손패를 통째로 치장해도 점수가 안 움직였다. 월 합을 키우는 길이 이것뿐이라 값이 커야 한 장이 손을 바꾼다.

| | 예전 | 지금 |
| --- | --- | --- |
| 먹칠 | +3 | **+25** |
| 금박 | +5 | **+40** |
| 무거운 달(카드 효과) | +8 | **+50** |
| 광약 | 광당 +10 | 광당 **+30** |
| 곳간 셈 | 버리기당 +14 | 버리기당 **+40** |

기본 덱에는 각인이 없으므로 **앞 스테이지는 거의 그대로**다. 차이는 덱을 짜고 난 뒤에 벌어지고, 그래서 뒤쪽 목표만 크게 올렸다.

기타 기준값: 제출 4회 · **버리기 3회** · 손패 8장 · 부적 슬롯 5칸 · 시작 자금 4냥 · 무한 달력 한 바퀴마다 목표 ×1.6.

버리기가 3인 이유: 그리디 시뮬레이션에서 제출 한 번은 라운드 점수의 약 25%를 만드는데 버리기 한 번은 3.5%밖에 못 만든다(4/4 → 4/3 로 줄여도 라운드 점수 1,180 → 1,096, −7%). 짓고땡은 8장 손패에서 2장 조합의 100%, 5장 조합의 65%가 이미 합법이라 "낼 게 없어서" 버릴 일이 거의 없고, 한 손의 점수 상한도 좁아 손패를 갈아도 크게 나아지지 않는다. 4:4로 두면 두 자원이 대등해 보이는 착시만 생긴다.

**기본값이 두 군데 있다.** `createInitialGameState`(타이틀 화면용)와 `applyStartDeck`(실제 런이 쓰는 값). 한쪽만 고치면 조용히 반영되지 않는다.

목표를 손대면 balance-probe 테스트가 같이 깨지므로, 곡선이 조용히 무너지지 않는다.

---

## 8. 장터

한 화면에 4분면. 두 단계로 들어가는 구조는 없앴다.

| 분면 | 파는 것 |
| --- | --- |
| 부적전 | 부적 2종. 런 내내 남아 매 손 자동 발동. **이미 가진 부적은 다시 뜨지 않는다** |
| 비결서점 | 비결서 2종. 해당 끗패 배수 레벨 영구 +1 |
| 덱 손질방 | 카드 묶음 2종 + 소각 1종 |
| 금단장 | 금단패 1종. 강하지만 영구적 대가가 따름 |

카드 묶음: 4냥 = 3장 중 1장을 덱에 추가, 7냥 = 5장 중 2장 추가. 나오는 카드에는 효과 태그가 무작위로 하나씩 붙는다(6절). 소각은 약 7냥에 덱에서 카드 하나를 영구히 뺀다.

**추첨은 균등 랜덤이다.** `generateShopOffers`의 `draw()`는 풀에서 고르게 뽑을 뿐 `rarity`와 `weight`를 읽지 않는다. 가중 추첨을 하는 `generateSeededOffers`(engine/economy.ts)는 리롤 경로와 테스트에서만 쓰인다. 즉 지금은 전설 금단패가 흔한 부적과 같은 확률로 뜬다. 의도한 것이라면 그대로 두고, 아니라면 `draw()`를 `generateSeededOffers`로 바꾸면 된다.

### 연계 조커

부적 53종 중 10종은 "혼자서는 값이 안 나오고 다른 카드가 있어야 의미가 생기는" 종류다. 일부러 서로 반대로 당기는 짝을 만들어 두었으니, 밸런스를 만질 때 짝을 같이 봐야 한다.

### 이름난 끗패 조커

끗패 하나만 보고 사는 물건이라 맞았을 때는 확실히 갚는다. 좁을수록 배수가 크다.

| 조커 | 조건 | 보상 |
| --- | --- | --- |
| 알리 부적 | 알리(1·2월) | 월 합 +40 · 배수 ×1.8 |
| 갑오 부적 | 갑오(끝자리 9) | 월 합 +45 · 배수 ×1.7 |
| 장땡 부적 | 장땡(10월 두 장) | 월 합 +70 · 배수 ×2.4 |
| 광땡 부적 | 광땡 세 종 아무거나 | 월 합 +90 · 배수 ×3 |
| 삼팔 봉인 | 38광땡만 | 월 합 +150 · 배수 ×5 |

같은 끗패의 **비결서와 겹쳐 사면** 기본 배수와 곱 배수가 같이 붙는다. 하나의 끗패를 파는 빌드가 성립하라고 일부러 그렇게 뒀다.

| 조커 | 읽는 것 | 대척점 |
| --- | --- | --- |
| 미련 | 손에 남긴 카드 수 | 다섯 손가락(5장 제출) |
| 달 지킴이 | 손에 들고만 있는 광 | 광약·광 곳간(광을 내야 값) |
| 곳간 셈 | 남은 버리기 횟수 | 버리기로 짓을 맞추는 플레이 |
| 삯꾼 주판 | 다른 부적들의 가격 합 | — (비싼 판을 짤수록 커짐) |
| 빼곡한 사당 | 빈 부적 칸이 0인지 | 빈 사당·빈 부적집(빈 칸이 값) |
| 첫맛 | 이번 런의 끗패 사용 횟수 | 비결서(한 족보를 반복하게 만듦) |
| 망통쟁이 | 끗패가 망통인지 | 짝패 매듭(땡을 원함) |
| 긴 짓 도둑 | 짓의 월 합 30 이상 | 외날·독장수(2장 제출) |
| 무광 연습 | 광 없이 낸 연속 손 | 광약·광 곳간 |
| 제물 단도 | 바로 오른쪽 부적을 삼킴 | 부적 배치 순서 전체 |

제물 단도와 무광 연습은 점수 계산이 아니라 리듀서 수명주기에서 처리된다(`resolveDevouringDaggers`는 `startStage`, 무광 성장은 `applyCardAftermath`).

---

## 9. 튜토리얼

1월이 튜토리얼 스테이지다. 발라트로식 스포트라이트 — 화면 전체를 어둡게 덮고 한 요소만 뚫어서 밝히며, 그 요소만 클릭된다. `box-shadow: 0 0 0 9999px`로 구멍을 내고 `.tutorial-target`의 z-index를 올려 클릭을 통과시킨다.

단계는 `tutorial-steps.ts`에 선언한다. **다음 버튼은 항상 있다.** `actionHint`는 버튼을 대체하는 게 아니라 옆에 붙는다. 예전엔 대체하는 구조였고, 그래서 지금 할 수 없는 행동을 안내하는 단계는 전부 막다른 길이 됐다 — 손에 낼 수 있는 짓이 없거나 곧 문턱을 놓칠 참이면 "안내 끄기" 말고는 빠져나갈 방법이 없었다. `doneWhen`이 있으면 그 행동을 하는 순간 자동으로 넘어가고, 아니면 다음을 눌러 넘어간다.

**구멍은 그림만 그린다.** `.tutorial-spotlight__hole`은 9999px 그림자로 화면을 덮는 역할만 하고 `pointer-events: none`이어야 한다. 이게 `auto`였을 때 구멍이 강조된 요소 위에 얹혀 **그 요소로 가는 클릭을 전부 삼켰다** — 누르라고 안내하는 바로 그 카드가 안 눌렸다. 차단은 구멍 바깥의 사각형 네 개(`.tutorial-spotlight__block`)가 맡는다.

**중요**: 현재 커서는 저장하지 않고 상태에서 파생시킨다. 따라서 모든 `doneWhen`은 **단조**여야 한다 — 한 번 참이 되면 다시 거짓이 되면 안 된다. 어기면 튜토리얼이 뒤로 돌아간다.

이걸 실제로 두 번 어겼다.

- 손패 단계가 `selectedCardIds.length >= 2`로 넘어갔다. 카드를 골랐다가 하나 빼면 그 단계로 되감겼다. → `doneWhen`을 없애고 다음 버튼으로 넘긴다. 뒤따르는 끗패·짓 단계가 고른 카드에 붙는 표식을 설명하므로 선택을 유지하는 편이 낫다.
- 제출 단계의 `roundSubmissionIndex >= 1`은 판이 바뀌면 0으로 돌아간다. 튜토리얼이 1월에만 붙어 있어 드러나지 않았을 뿐이다. → 모든 단계에 `|| stage >= 2` 탈출구를 둔다.

`game/tests/tutorial.test.ts`가 이 두 가지를 막는다. 카드 선택을 넣었다 뺐을 때 뒤집히는 `doneWhen`이 있는지, 그리고 판 진행 타임라인을 따라 걸을 때 참→거짓으로 되돌아가는 단계가 있는지 검사한다.

React `ref`로 고수위선을 만들어 우회하려 하지 말 것. 렌더 중 ref 접근은 `react-hooks/refs`가 막고, 그건 정당한 지적이다.

---

## 10. 알려진 함정

- **`game/content/upgrades.ts`는 CRLF다.** 다른 파일은 LF. 스크립트로 편집할 때 줄바꿈을 맞추지 않으면 문자열 검색이 조용히 실패한다.
- **CSS를 스크립트로 편집하지 말 것.** 이 저장소에서 세 번 깨졌다. `.hwatu-card__hint {`는 `.hwatu-card:focus-visible .hwatu-card__hint {`의 부분 문자열이라 단순 치환이 호버 선택자를 쪼갠다. 여러 줄 `:where(...)` 안의 한 줄을 지우면 괄호가 깨진다. 손으로 고치거나, 최소한 PostCSS 파서 + 바이트 수 + 규칙 수로 검증하라.
- **React `set-state-in-effect` 린트.** 이펙트에서 상태를 넣지 말고 파생시켜라. 튜토리얼 커서와 스포트라이트 위치가 이것 때문에 두 번 걸렸다.
- **부동소수 잔재.** `1000 × 1.5 × 1.1 = 1650.0000000000002`. `getGoRequirement`는 `Number(x.toFixed(6))` 뒤에 `Math.ceil`한다.
- **`node_modules`는 OS별 네이티브 바이너리를 포함한다.** 윈도우에서 설치한 폴더를 리눅스로 옮기면 rollup/rolldown/esbuild가 전부 터진다. 플랫폼마다 새로 `npm install`하라.

---

## 11. 남은 일

- 카드 효과 태그 12종의 실제 엔진 구현 (6절). 사용자가 직접 할 예정
- 이미지 연결. `data-asset-tag`가 붙은 자리에 사용자가 한 장씩 붙임 → `docs/ASSET_REPLACEMENT_GUIDE.md`
- 두목 12종 중 짓고땡 전환 뒤 검증하지 않은 것들. 특히 `boss_reverse_calendar`(월을 뒤집으면 짓 계산이 통째로 달라진다)
- 부적 53종 중 옛 포커식 족보를 전제로 수치를 잡은 것들의 재조정
- 상점 추첨 가중치(위 8절). 지금은 `weight`·`rarity`가 장식이다
- 이미지 CSS 훅. 화투패와 수집판 아이콘 2곳만 연결되어 있고 부적·묶음·비결서·두목·스테이지 배경은 `data-asset-tag`만 붙어 있다
